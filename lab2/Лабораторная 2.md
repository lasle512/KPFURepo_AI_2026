# Лабораторная работа №2: Метод k-ближайших соседей и непараметрические методы регрессии

**Время на выполнение:** 4 академических часа (2 пары) + самостоятельная работа.

## 1. Цель работы

Освоить применение непараметрических алгоритмов машинного обучения (kNN и Ядерная регрессия Надарая-Ватсона), изучить влияние метрик расстояния и гиперпараметров на качество прогнозирования реальных экономических показателей.

## 2. Формат сдачи и требования к репозиторию

Все работы сдаются строго в виде ссылки на ваш публичный репозиторий на GitHub.

**Структура репозитория:**
В вашем репозитории должна быть создана папка для второй лабораторной работы (`Lab_2`), содержащая:

1. `README.md` — краткое описание проекта, выбранного датасета, бизнес-задачи и итоговых аналитических выводов.
2. Файл с датасетом `.csv` (если файл превышает лимиты GitHub, укажите прямую ссылку на Kaggle в README).
3. `ФИО_Группа_Lab2.ipynb` — Jupyter Notebook с полностью исполненным кодом, построенными графиками и текстовыми Markdown-пояснениями к каждому этапу.
4. `requirements.txt` — зафиксированные зависимости виртуального окружения.

## 3. Выбор датасета

Поскольку методы, изученные на практике, отлично подходят для задачи регрессии (прогнозирования непрерывной стоимости или расходов), выберите один из предложенных экономических датасетов на Kaggle:

- **House Prices - Advanced Regression Techniques**: Классическая задача прогнозирования стоимости недвижимости на основе десятков характеристик дома и участка.
- **Used Car Price Prediction**: Предсказание рыночной стоимости автомобилей с пробегом (на основе года выпуска, пробега, типа топлива, трансмиссии и т.д.).
- **Supermarket / Retail Store Sales Prediction**: Прогнозирование объема продаж или выручки магазина на основе характеристик торговой точки и товара.

## 4. Задачи для самостоятельной реализации

Опираясь на код из практического занятия, вам необходимо самостоятельно реализовать логику непараметрического моделирования для ваших данных:

1. **Загрузка и Разведочный анализ (EDA)**:
   - Загрузите данные и очистите их от неинформативных признаков (ID, уникальные имена).
   - Сгруппируйте данные по категориальным признакам и сделайте экономические выводы (например, как средняя цена квартиры зависит от района).
2. **Подготовка признаков**:
   - Переведите категориальные переменные в числа (One-Hot Encoding).
   - Сделайте разбиение выборки на Train и Test.
   - Обязательно отмасштабируйте числовые признаки (`StandardScaler`), избегая утечки данных.
3. **Поиск оптимального числа соседей ($k$)**:
   - Напишите цикл для перебора нечетных значений $k$ (например, от 1 до 25) для модели `KNeighborsRegressor`.
   - Сохраните ошибки (MAE) для каждого $k$ и постройте линейный график зависимости ошибки от числа соседей. Сделайте вывод о том, какое $k$ лучше защищает от переобучения.
4. **Сравнение метрик расстояния Минковского**:
   - Обучите две модели kNN с оптимальным $k$, найденным на предыдущем шаге. В одной используйте Евклидово расстояние ($p=2$), в другой — Манхэттенское ($p=1$).
   - Сравните их метрики (MAE и $R^2$) и объясните результат.
5. **Ядерная регрессия Надарая-Ватсона**:
   - Реализуйте метод с использованием Гауссовского ядра (`rbf_kernel`).
   - Напишите цикл для перебора гиперпараметра `gamma` (отвечающего за ширину окна).
   - Найдите лучшую `gamma`, максимизирующую $R^2$, и выведите итоговые метрики.
6. **Визуализация и бизнес-вывод**:
   - Постройте Scatter-график «Реальные значения vs Предсказанные значения» для вашей лучшей модели, добавив идеальную диагональ.
   - **Финальный бизнес-вывод**: Опишите, насколько хорошо непараметрическая модель справилась с задачей ценообразования и как бизнес может применять эту модель (например, для автоматической оценки стоимости б/у авто).

## 5. Контрольные вопросы для защиты

1. В чем философское и математическое отличие параметрических алгоритмов машинного обучения (например, линейной регрессии) от непараметрических (kNN)?
2. Как работает метрика Манхэттенского расстояния и чем она отличается от Евклидовой?
3. Что такое «Проблема баланса смещения и разброса» (Bias-Variance Tradeoff) при выборе гиперпараметра $k$ в алгоритме kNN? К чему ведет выбор $k=1$?
4. В чем заключается суть «Проклятия размерности» для метрических алгоритмов при работе с датасетами, где сотни признаков?
5. В чем ключевое отличие регрессии Надарая-Ватсона с Гауссовским ядром от стандартного метода k-ближайших соседей?

https://www.kaggle.com/datasets/therohithanand/used-car-price-prediction
